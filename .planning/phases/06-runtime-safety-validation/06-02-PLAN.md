# Plan 06-02: Measure Core 1 Stack Headroom and Verify READALL

## Objective

Measure Core 1 stack headroom during realistic USB Host + TinyUSB + MIDI load and verify `READALL` protocol responses on real hardware.

## Scope

- Instrument or otherwise measure Core 1 stack usage during peak routing activity.
- Execute Web Serial `READALL` requests repeatedly while debug output remains non-interfering.
- Confirm response completeness and JSON integrity.

## Tasks

1. Implement stack-usage measurement approach suitable for RP2040 runtime checks.
2. Run load and capture max observed usage with margin analysis.
3. Execute and validate multiple `READALL` cycles; record results.

## Verification

- Documented Core 1 stack headroom is within acceptable margin for current workload.
- `READALL` responses are complete and parseable across repeated runs.
